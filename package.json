{
  "name": "retry-llm",
  "version": "1.0.0",
  "description": "Retry for LLM API calls — rate limits, model fallback, token budgets",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "keywords": ["retry", "llm", "ai", "rate-limit", "anthropic", "openai", "fallback", "token-budget"],
  "license": "MIT",
  "author": "Saad Naveed",
  "repository": {
    "type": "git",
    "url": "https://github.com/saadnvd1/retry-llm.git"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "@types/bun": "latest"
  }
}
